version: '3.8'
services:
    notification-service:
        build: .
        volumes:
            - ./src:/usr/app/src
        ports:
            - '3000:3000'
        command: yarn test
        depends_on:
            - mongo
    mongo:
        image: mongo:4.2.8-bionic
        ports:
            - '3001:27017'
    # mongo-seed:
    #     build: ./mongo-seed
    #     links:
    #         - mongo
